general {
        colors = true
        interval = 5
}

order += "load"
order += "cpu_temperature 0"
order += "disk /"
order += "time"

time {
	format = "%Y-%m-%d %H:%M:%S"
}

load {
	format = "%1min"
}

cpu_temperature 0 {
	format = "T: %degrees °C"
}

disk "/" {
	format = "%free"
}
